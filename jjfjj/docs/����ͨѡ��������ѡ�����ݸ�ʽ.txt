1、关键词列表数据获取方法：
$.ajax({
    url: '/getProductKeyList/', //url中带productId
    type: "get",
    success: function (res) {
		if (res.success) {
			//todo
		}
    }
});
ajax回调返回数据格式：
[{
	id: 123,
	key: '关键词', //关键词
	pv: 123, //展现量
	clk: 1232, //点击数
	price: 248, //市场均价
	t1: 23, //竞争度
	t2: 32 //推荐度
},{
	id: 123,
	key: '关键词', //关键词
	pv: 123, //展现量
	clk: 1232, //点击数
	price: 248, //市场均价
	t1: 23, //竞争度
	t2: 32, //推荐度
	st: 1 //1:展现量升序2:展现量降序,
	      //3:点击数升序4:点击数降序,
	      //5:均价升序6:均价降序,
	      //7:竞争度升序8:竞争度降序,
	      //9:推荐度升序10:推荐度降序,
}]